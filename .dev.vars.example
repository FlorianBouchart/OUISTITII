# Copier en `.dev.vars` pour le développement local (fichier ignoré par git).
# Sans clés S3, l'app bascule automatiquement en mode "relais" : les fichiers
# transitent par le Worker et atterrissent dans le bucket R2 local de Wrangler.
TOKEN_SECRET=dev-secret-a-remplacer
ADMIN_PASSWORD=ouistitii
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
